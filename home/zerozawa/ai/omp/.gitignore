# SQLite databases — session state, history, credential cache
agent.db
agent.db-shm
agent.db-wal
history.db
history.db-shm
history.db-wal
models.db
models.db-shm
models.db-wal

# Session storage
sessions/

# Terminal session cache
terminal-sessions/

# Logs
logs/

# GPU detection cache
gpu_cache.json

# Native binaries (version-locked, auto-generated)
natives/

# Extensions dependencies
agent/extensions/node_modules/
agent/extensions/README.md

node_modules

# Plugin cache (downloaded marketplace content)
plugins/cache/